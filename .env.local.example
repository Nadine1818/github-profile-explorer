# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Used for the AI summarize + chat features (app/api/summarize, app/api/chat).
# create one at https://console.groq.com/keys
GROQ_API_KEY=
